version: "3.8"

services:
  palace-intrigue:
    build: .
    ports:
      - "6891:6891"
    env_file:
      - .env
    environment:
      - PORT=6891
    volumes:
      - ./data:/app/data
      - ./agents:/app/agents:ro
    restart: unless-stopped
